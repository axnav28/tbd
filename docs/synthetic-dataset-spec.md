# Synthetic dataset specification

The Phase 3 generator creates a clearly labelled fictional Northstar Finance NBFC dataset with 34 assets, 16 real CVE identifiers, four KEV examples, identities, and the controls named by the problem statement. It never represents generated records as enterprise telemetry. EPSS/KEV refresh from public services remains the Phase 5 live-intelligence responsibility; the current catalog carries deterministic demo values and provenance fields for the Phase 3 seed.
