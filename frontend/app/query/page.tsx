"use client";
import { useState } from 'react';
import { Shell } from '../../components/ui/shell';
export default function QueryPage() { const [question, setQuestion] = useState('Which fix reduces the most shared-path risk?'); return <Shell eyebrow="Graph query / Evidence-linked" title="Ask the graph"><div className="grid"><section className="panel wide"><div className="section-kicker">Natural language query</div><div className="form-row"><input className="query-input" value={question} onChange={(event) => setQuestion(event.target.value)} /><button className="action">Run query</button></div><div className="panel section-space"><div className="section-kicker">Answer · synthetic graph</div><p className="empty">The VPN gateway is the highest-leverage shared node in the current model. Patching the loan-origination path also partially resolves the payment-adjacent scenario.</p><span className="node">↳ Citation: path S1 · INTERNET → VPN GATEWAY → PAYMENT-ADJACENT</span></div></section></div></Shell>; }
