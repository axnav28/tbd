"use client";
import { useState } from 'react';
import { Shell } from '../../components/ui/shell';
import { ConfidenceBadge } from '../../components/confidence-badge';
export default function OptimizerPage() { const [budget, setBudget] = useState(250000); return <Shell eyebrow="Capital allocation / Bounded LP" title="Investment optimizer"><div className="grid"><section className="panel wide"><div className="panel-head"><div><div className="section-kicker">Scenario planning</div><div className="panel-title">Risk reduction per rupee</div></div><ConfidenceBadge value="Configured" /></div><div className="budget-line"><span>Available budget</span><strong>₹{budget.toLocaleString('en-IN')}</strong></div><input className="range" type="range" min="0" max="1000000" step="10000" value={budget} onChange={(event) => setBudget(Number(event.target.value))} /><div className="chart-wrap optimizer-bars"><div style={{height:'42%',width:'100%'}}><span>Patch deployment · 80%</span></div><div style={{height:'27%',width:'80%'}}><span>MFA admins · 55%</span></div><div style={{height:'15%',width:'62%'}}><span>EDR coverage · 30%</span></div></div></section><section className="panel side"><div className="section-kicker">Recommended action</div><h2 className="panel-title">Patch deployment</h2><div className="big-number">₹0.50M</div><ConfidenceBadge value="Estimated" /><p className="empty">Quantified reduction across the loan-origination path. Also partially resolves the payment-adjacent scenario via the shared gateway.</p><button className="action">View affected paths</button></section></div></Shell>; }
